version: "3"

services:
  integration-tests:
    image: cr:integration-tests
    depends_on:
      - api
      - stats_updater
    env_file:
      - test.env
      - db.env
      - rabbit.env
    networks:
      main:
        aliases:
          - integration_tests